watch_defaults = { base_poll_seconds = 1800, category = "finance", check_method = "get", content_type = "text/html", detectors = [
  "content_hash",
  "element_hash",
], emit_mode = "new_items_only", fallback_to_get = true, fetch_body_on_change = true, item_identity = "href", language = "en", max_body_bytes = 1048576, max_items_per_fetch = 10, normalize_whitespace = true, provenance = "hindenburg", strip_query_params = true, tags = [
  "activism",
  "reports",
  "short-research",
] }

[[watch_profiles]]
exclude_selectors = [
  ".menu",
  ".nav-links",
  "footer",
  "header",
  "nav",
  "script",
  "style",
]
item_selector = ".post-title a[rel='bookmark'], .featured_post h1 a"
link_selector = "a[href]"
name = "hindenburg-home-reports"
published_format = "%+"
published_selector = "time.entry-date"
title_selector = ".post-title, h1"

[[watches]]
emit_title = "Hindenburg Research updates"
id         = "hindenburg-home"
profile    = "hindenburg-home-reports"
url        = "https://hindenburgresearch.com/"
